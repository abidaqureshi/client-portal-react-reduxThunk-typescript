import theme from "../../theme";

export const breakpoints = {
    xs: `(min-width: ${theme.metrics.breakpoints.xs}px)`,
    sm: `(min-width: ${theme.metrics.breakpoints.sm}px)`,
    md: `(min-width: ${theme.metrics.breakpoints.md}px)`,
    lg: `(min-width: ${theme.metrics.breakpoints.lg}px)`,
    xl: `(min-width: ${theme.metrics.breakpoints.xl}px)`,
};